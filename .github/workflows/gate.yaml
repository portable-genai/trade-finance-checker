# GENERATED by org-metadata/scripts/render-actions-callers.py from the reviewed job
# contract in ci/gcp/repository-policy.json. Do not edit this file: `--check` fails the
# gate when it differs from what the contract renders, which is the control that stops
# the drift that rotted the workflows retired on 2026-09-01.
#
# There is deliberately no `paths-ignore`. The Cloud Build triggers skipped `**/*.md`
# and `docs/**` to save build minutes, and the recorded consequence was that a docs-only
# pull request reported NEUTRAL and merged with the gate never running. Actions on a
# public repository costs nothing, so that trade no longer buys anything -- and a skipped
# workflow never reports at all, which blocks a pull request once the check is required.
name: gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: gate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  demo-browser:
    uses: portable-genai/.github/.github/workflows/fleet-gate.yaml@c8e3eb537e377c6590372645a2ff5e8c5928fcd0
    with:
      runner_image: "sha256:075c71f47bc88a36ab7db5b4051adcd73370adf9774f1247771172d2d197fef1"
      runner_release_tag: "runner-python3.14-node24"
      runner_archive_sha256: "90c0ea91161b5bb6aa603c0d4a7b817fcf9ddf4ff235a14c9bdfc3327eb58eb9"
      job_id: "demo-browser"
      make_targets: "demo-browser"
      extra_lockfiles: "requirements-demo.lock"
      extra_lockfiles_first: true
      demo_browser_required: true

  lint-gcp:
    uses: portable-genai/.github/.github/workflows/fleet-gate.yaml@c8e3eb537e377c6590372645a2ff5e8c5928fcd0
    with:
      runner_image: "sha256:075c71f47bc88a36ab7db5b4051adcd73370adf9774f1247771172d2d197fef1"
      runner_release_tag: "runner-python3.14-node24"
      runner_archive_sha256: "90c0ea91161b5bb6aa603c0d4a7b817fcf9ddf4ff235a14c9bdfc3327eb58eb9"
      job_id: "lint-gcp"
      runtime_lockfile: "requirements-gcp.lock"
      lint_gcp: true

  offline-gate:
    uses: portable-genai/.github/.github/workflows/fleet-gate.yaml@c8e3eb537e377c6590372645a2ff5e8c5928fcd0
    with:
      runner_image: "sha256:075c71f47bc88a36ab7db5b4051adcd73370adf9774f1247771172d2d197fef1"
      runner_release_tag: "runner-python3.14-node24"
      runner_archive_sha256: "90c0ea91161b5bb6aa603c0d4a7b817fcf9ddf4ff235a14c9bdfc3327eb58eb9"
      job_id: "offline-gate"
      make_targets: "check ui-check demo-selftest portability"
      npm_directory: "ui"
      npm_directory_first: true
