name: Eval gate

# The pre-merge eval SMOKE check, and NOT the promotion authority. Runs
# `eval/run_eval.py --mode smoke` over the golden presentation set and fails (non-zero exit) if any
# gated metric is below its threshold. The metric list and the thresholds have one home,
# `eval/rubrics/*.yaml`, which `load_thresholds_from_rubrics()` reads, so they are not
# restated here. The promotion VERDICT is Hrz4's, run separately by promotion-gate.yaml at
# release time (`--mode gate`). Workflow name kept stable for branch protection.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: eval-gate-${{ github.ref }}
  cancel-in-progress: true

env:
  # The smoke check runs deterministically on the local profile (its own inlined offline
  # adapters), so it needs no cloud credentials. It cannot be turned into the promotion
  # verdict by switching profile here: `--mode gate` is a separate workflow,
  # promotion-gate.yaml.
  TRADE_FINANCE_PROFILE: local

jobs:
  eval:
    name: eval smoke check (pre-merge)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4

      - name: Set up Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.lock && pip install -e . --no-deps

      - name: Run eval smoke check
        # Non-zero exit fails the pre-merge check. The authoritative promotion verdict is
        # Hrz4's (promotion-gate.yaml, --mode gate).
        run: python eval/run_eval.py --mode smoke
